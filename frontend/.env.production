# Used only for `npm run build`. Since the frontend is served by the same
# Spring Boot app in production, API calls go to a relative path on the
# same origin — no separate backend URL needed.
VITE_API_BASE_URL=/api
